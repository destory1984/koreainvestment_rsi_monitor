# 개발 노트

> 2026-09-26 기준 (버전 0.2). 00:50 에 컨텍스트를 비운 뒤 43~46번(다이버전스 더 시험, 사전 등록 판정 준비, 자동 시작 확인, 알림 목소리 Edge)을 했다. 새 세션이 이 파일만 읽고 이어서 작업할 수 있게 쓴다.
> 09-25 밤~09-26 새벽에 한 것은 「지금까지 한 것」 33~42번이다 (대부분 백테스트). 컨텍스트가 길어져 사용자가 clear 한다.
> 할 일은 [TODO.md](TODO.md), 쓰는 법은 [README.md](README.md). **사용자와의 대화는 조선시대 왕과 신하 말투로 한다** (사용자는 「전하」, Claude 는 신하 — 「~하옵니다」, 「~하였사옵니다」. 09-25 사용자가 정함).
> 문서(README·NOTES·TODO·코드 주석)와 커밋 메시지는 전처럼 쉬운 평서체로 쓴다.

## 지금 상태

- 저장소: https://github.com/destory1984/koreainvestment_rsi_monitor (공개). 로컬 `C:\_c\koreainvest`, 브랜치 `main`.
  09-26 00:40 `1f67d67` 까지 push 됨 (이 NOTES 커밋은 아직). 09-25 밤 커밋들은 시작 인사 목소리 노트를 빼느라 다시 써서 강제 push 했다
  (사용자가 스크립트를 직접 돌림, 옛 것은 로컬 가지 `backup-before-scrub`). **목소리 이야기는 저장소 밖 `NOTES.local.md` 에만 적는다 — 저장소는 공개.**
  push 전에는 `git grep` 으로 목소리 이야기·키가 섞이지 않았는지 본다.
- **커밋은 기능 하나 끝날 때마다 묻지 말고 한다. push 는 사용자가 시킬 때만.** 커밋 끝에 `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`.
- 웹 서버는 사용자가 띄워 둔다 (09-25 22:10 쯤 다시 켬 — 32번까지 들어가 있다. **33번 조기 폐장, 37번 `/report`, 39번 넥스트레이드 스위치는
  서버를 다시 켜야 적용** — 사용자에게 다시 켜기를 권할 것). **Claude 가 새 서버를 띄우지 않는다** — 앱키 하나에 실시간 연결 하나라 사용자 서버가 끊긴다.
  사용자가 「서버 다시 켰어, 확인해줘」 하면: `/api/state` 가 200 이 될 때까지 기다리고(켜는 데 20~30초), 바뀐 것을 API·화면(localhost 탭 새로고침)으로 본다.
  코드를 고치면 "서버를 다시 켜야 적용"이라고 알린다. `static/index.html` 만 고친 것은 새로고침으로 된다.
- 로컬 TTS 서버는 **늘 띄우지 않는다.** 녹음할 때만 `python tts_make.py` 가 띄웠다 내린다 (Claude 가 돌린다).
- 윈도우 작업 스케줄러 두 개 (사용자 허락받고 등록):
  - `KIS RSI 모니터` — 로그인 30초 뒤 `pythonw kis_web.py --no-browser --log kis_web.log`. 이미 떠 있으면 스스로 끝난다.
  - `KIS 주간거래 분봉 수집` — 평일 09:00~18:30, 30분마다 `collect_day.vbs` → `kis_replay.py --collect`. 기록은 `replay_cache/collect.log`.
    09-25 부터 **모든 종목의 1분봉**도 이어 쌓는다 — 모니터링 13종목 + `kis_collect.json` 18종목 = 31종목 (36번).
    새 18종목은 09-28(월) 09:00 첫 수집에서 종목당 50초쯤 걸려 25거래일 치를 받는다.
- Claude 예약 작업 둘 (`C:\Users\eofeo\.claude\scheduled-tasks\<id>\SKILL.md`, 둘 다 신하 말투로 알리게 해 둠):
  - `kis-night-bars-check` — **2026-09-28(월) 08:30** 한 번. 목요일 밤 주간거래 세션 분봉이 다음 세션 직전까지 받아지는지 보고 NOTES 에 적고,
    30분 수집을 줄일지 사용자에게 묻는다. (앱이 꺼져 있으면 다음에 켤 때 돈다 — 그러면 세션이 이미 열려 확인이 어긋난다.)
  - `kis-replay-review` — **2026-10-02(금) 17:30** 한 번. `python kis_report.py --fetch` 로 보고서를 새로 만들고, 되감기 채점 + 실제 알림 성적을 보고,
    시그널 방아쇠(29번)·종목별 선(30번)·A 추세 필터·변동폭 문턱을 사용자에게 묻는다. 판단 근거는 「나눠서 확인」·날 단위 90% 구간·비용 뺀 값 (40번).
- Claude 쪽 설정: devin MCP 연결(403 으로 안 쓰였고 컨텍스트 3만 토큰)을 09-26 사용자 요청으로 지웠다 (`claude mcp remove devin -s user`).
  인증 헤더가 든 원래 설정은 `C:\Users\eofeo\.claude\devin-mcp-backup.json` — 다시 쓰려면 이것으로 되살린다.
- 추석 연휴(09-24~26)라 국내 장은 쉰다(화면엔 「휴장」). 다음 국내 장은 09-28(월). 미국 주간거래는 한국 낮에 돈다.
- **백테스트 현황 한 줄**: 13종목 20일(08-28~09-25)로는 지금 알림(5분봉 35/65)이 기준보다 조금 낫지만(+0.05%) 우연과 못 가르고 비용 0.2% 를 빼면 −.
  가장 나은 후보는 A 추세 필터(42번, 앞·뒤 절반 모두 +)와 변동폭 문턱(40번). 다이버전스는 오히려 나빴다(41번), 히든·긴 봉·손절익절로도 비용을 넘는 것은 없었다(43번). 규칙은 10-02 한 달 치 뒤 사용자가 정한다.

## 지금까지 한 것

1. **분봉·실시간** (`kis_rsi.py`) — 미국 분봉 HHDFS76950200(120개씩, `keyb` 로 거슬러, 한 달쯤까지), 실시간 HDFSCNT0.
   국내는 1분봉 FHKST03010230 을 묶고 실시간 H0STCNT0. 현재가 HHDFS00000300(`fetch_quote`).
2. **RSI·MACD** — 와일더 RSI(14), MACD(12,26,9). Webull 과 09-24 장중 66건 차이 중앙값 0.18.
3. **터미널 명령** — `bars` / `live` / `rsi` / `watch` / `setup`.
4. **웹 화면** (`kis_web.py` + `static/index.html`) — 서버 `Hub` 가 실시간 연결을 잡고 0.5초마다 바뀐 종목만 웹소켓으로 보낸다.
   종목 표, 5분봉·RSI·MACD 차트, 알림 기록, 지수 띠.
5. **소리 알림** (`kis_alert.Gate`) — 35/65(땡+말), 30/70(세 음+말), 재무장 7, 쿨다운 10분. 서버 PC 스피커.
6. **매수·매도 시그널** (`kis_signal.py`) — RSI 선을 넘으면 12봉 무장, 닫힌 봉 RSI 가 선 안으로 돌아오면 (29번에서 바꿈. 처음엔 MACD 히스토그램 부호). 강/약, 추세 순응/역추세.
7. **지수 띠** — 맨 앞에 미국·한국 장 상태와 남은 시간, 그다음 S&P500 · 나스닥 · 코스피 · 원/달러 · 비트코인(업비트) · 니케이.
8. **화면 편의** (09-25) — 손잡이(⋮⋮)로 줄 차례 바꾸기(`PUT /api/order`), 「칸 ▾」 칸 고르기, 「가-」「가+」 글자 크기 9~18px
   (둘 다 브라우저 localStorage), 「⚙ 설정」 메뉴(지표 글, RSI 선 알림·매수·매도 시그널 스위치 + ⓘ 자세히, 소리 나는 때, TTS 테스트).
   종목 이름은 RSI 추이(▲ 빨강 ▼ 파랑) 색. 미국 가격은 1달러 이상 소수 둘째, 밑이면 넷째. RSI 는 정수(버림).
9. **미국 주간거래(한국 낮)** — 미국 동부 20:00~04:00 에는 실시간을 `R`+`BAQ/BAY/BAA` 키로 받는다(30초마다 시각 보고 D↔R 바꿔 걸기).
   가격 앞에 「주간」. 등락은 **전일 정규장 종가 대비**(한국투자증권이 주는 값 그대로, 09-25 역산으로 확인).
10. **오버나이트 봉을 RSI 에 넣을지 = Webull 따라가기** — Webull 은 24시간 거래 종목만 5분봉에 오버나이트 봉을 넣는다
    (MU 넣음: 우리 34.70 vs Webull 72.20 → 넣으니 맞음. FCEL 안 넣음: 빼면 51.95 vs Webull 51.92).
    목록을 알 길이 없어 **최근 오버나이트 5분 칸 96개 중 절반 넘게 체결**이면 넣는다. RSI 옆 「24h」/「정규」, 누르면 까닭과 바꾸기(설정 `night`).
    09-25 기준 넣음: SOXL·KORU·DRAM·MU·SNDK·TSLA·BE·LITE, 뺌: FCEL(6)·GEV(5)·POET(31).
11. **알림 채점** (`kis_replay.py`) — 분봉을 거슬러 받아 같은 `Gate`·`signals` 로 되감고 15·30·60분 뒤 방향을 센다(기준·초과 포함).
    서버 알림 기록과 대조: 알림 15 중 12, 시그널 7 중 6 이 같은 봉. 첫 결과는 아래 「채점 결과」.
12. **주간거래 분봉 수집** (`--collect`) — 한국투자증권이 주간거래 분봉을 가장 최근 세션 것만 주니 모아 둔다 (「아직 확인 못 한 것」 참고).
13. **알림 시간대** — 설정의 「소리 나는 때」: 미국 주간거래·프리장·정규장·애프터·국내 종목 체크, 조용한 시각(자정 넘어도 됨).
    가린 알림은 기록에 「🔇 까닭」. 설정 `sound_sessions`, `quiet`.
14. **자동 시작** — 위 작업 스케줄러. `--log`, 중복 실행 막기, 키를 `~/.bashrc` 에서도 읽기.
15. **켤 때** — 미국 종목 현재가·등락을 한 번 물어 채운다. 켤 때 이미 선 너머인 알림은 「🔇 켤 때 이미 넘어 있음」으로 기록만.
16. **여러 시간봉 RSI** — 「1·5·15·60」 칸. 종목마다 1·15·60분 `Book` 을 따로 두고(`Hub.tf`) 체결을 같이 넣는다. 켤 때 뒤에서 받는다.
17. **목소리** — 로컬 TTS(Qwen3-TTS 1.7B, Sohee) → Edge(SunHi, +20%) → SAPI. BE 는 「블룸에너지」. 캐시 86개 모두 로컬 `.wav`.
    `tts_make.py` 가 로컬 TTS 서버를 잠깐 띄워 없는 문장만 녹음하고 내린다.
18. **버전 0.2** (제목).
19. **종목만 소리 끄기** (09-25) — 줄 끝 종 단추(`POST /api/mute`), 설정 `mute`. `muted()` 가 맨 먼저 본다. 이름 앞 🔇 를 눌러도 켠다.
20. **국내 종목 되감기** (09-25) — `kis_replay.py` 가 국내도 한다. 1분봉을 `fetch_kr_bars(need=days*133)` 로 받아 캐시에 합친다.
    (처음엔 넥스트레이드 애프터를 「NXT」 세션으로 따로 셌다가 21번에서 뺐다.)
    「맞음」은 보합을 빼고 세고 「보합」 칸을 더했다 (삼성전자 15분 뒤 22% 가 보합이라 맞음이 낮게 나왔다).
21. **국내 분봉은 KRX 정규장만** (09-25) — `fetch_kr_bars` 가 1분봉 09:00~15:30(`KRX_OPEN`/`KRX_CLOSE`)만 남긴다.
    분봉 API 는 넥스트레이드 애프터(15:30~20:00, SK하이닉스는 09-14 부터)도 주는데 실시간 H0STCNT0 은 KRX 만이라,
    켜 둔 서버엔 그 봉이 없고 새로 켠 서버엔 있어 다음 날 RSI 가 달랐다. 거슬러 받는 쪽수는 두 배로(다 모이면 멈춤).
    되감기도 캐시에 있던 NXT 봉을 뺀다.
22. **알림 뒤 결과** (09-25) — `Hub.fill_after` 가 알림·시그널마다 15·30·60분 뒤를 채운다(`ev["after"]`, 키는 문자열 분, + 면 맞음, 장 닫힘은 null).
    들어간 값은 알림 때 `price`(새로 적는 칸, 옛 기록은 알림 봉 종가), 나온 값은 알림 봉 + h분에 시작한 봉이 닫힌 뒤 종가(10분 안에 없으면 null).
    봉이 닫힐 때(`closed`)와 켤 때 한 번 돈다. 셋 다 정해지면 `kis_alerts.jsonl` 에 `{"type": "after", "of": ts, "symb", "after"}` 한 줄 → `read_log` 가 붙인다.
    억제·켤 때(`start`, 새로 적는 칸) 알림은 뺀다. 웹소켓 `{"type": "after"}`.
23. **휴장일** (09-25) — 국내는 국내휴장일조회 CTCA0903R(`kr_holidays`, 오늘부터 24일, `opnd_yn`)을 하루 한 번, `kis_holidays.json` 에 둔다.
    미국은 `holidays.NYSE()`(뉴욕증권거래소 종일 휴장 규칙, 조기 폐장은 33번) + `us_closed` — 처음엔 손으로 적은 2026~2027 목록이었고 같았다. `us_session`·`us_day_session` 이 휴일을 안다.
    미국 밤 세션은 다음 날 장에 딸린 것으로 보고 다음 날이 휴일이면 쉰다고 본다 — **확인 못 함** (노동절 전날 밤 등에서 볼 것).
    서버가 `state.holidays`·웹소켓 `holidays` 로 주고, 화면 장 상태가 「휴장 (휴일)」.
24. **규칙 시험** (09-25) — `tests/test_rules.py`, 표준 `unittest`(pytest 없음). Gate·시그널·`muted`·`set_mute`·`fill_after`·`read_log`·
    세션·휴장일·`fetch_kr_bars`(가짜 응답)·되감기 채점. 일부러 규칙을 망가뜨리면(재무장 폭 0, 결과 부호 반대) 잡는 것을 봤다.
25. **텔레그램** (09-25) — `kis_telegram.py`. 토큰·대화방은 환경변수 `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`(Webull 판 `C:\_c\rsi\rsi_monitor.py` 가
    setx 로 넣어 둔 것, 이 PC 에 있다) → `kis_config.json` 의 `telegram_token`/`telegram_chat`. `setup`·`test` 명령. 스레드 줄로 보낸다.
    억제·켤 때 알림은 안 보내고, `muted` 면 `disable_notification`. 설정 `telegram`, API `/api/telegram` `/api/telegram/test`.
    오류 글에서 토큰을 지운다. 09-25 사용자 허락받고 「보내 보기」(`POST /api/telegram/test`) 한 통 → `ok: true`.
26. **분봉 캐시를 SQLite 로** (09-25) — `replay_cache/bars.db`, 표 `bars`(excd, symb, nmin, t, OHLCV, 키 앞 넷)·`tickers`(night).
    WAL, 기다림 30초. JSON 13개(봉 21,820)를 옮겼고 원본은 `replay_cache/json_backup/`. 옮기기 전후 되감기 출력이 같았다.
    3.3MB → 1.8MB. 까닭: 30분 수집과 되감기가 겹쳐도 안전, 새 봉만 넣으니 기간이 길어도 쓰기가 늘 몇 ms.
    **쓰는 동안 DB 가 잠기니 네트워크는 다 받은 뒤에 쓰고 곧 commit** (`load_bars`). `time_kr` 은 안 넣는다.
    미국 5분봉은 한국투자증권이 5주쯤(TSLA 08-21~)까지만 주고, 국내 1분봉은 6개월(03-27~)도 준다.
27. **서버도 분봉을 DB 에** (09-25) — `Hub._bars` 가 받은 봉(진행 중인 마지막 봉 빼고)을 `store` 로 넣는다. 정규 쪽은 덮어쓰고
    주간거래 쪽은 빈 자리만, `tickers.night` 도 서버 판단대로. 봉이 닫히면 `store_closed` 가 닫힌 봉을 빈 자리일 때만 넣는다
    (실시간 봉은 API 봉을 덮지 않는다). 쓰기는 다른 스레드 — DB 가 잠겨 있어도 알림이 늦지 않게. 못 써도 서버는 돈다.
    KORU 로 실제 확인: 1643 → 1693봉. 되감기 수집(`--collect`)은 서버가 꺼져 있을 때를 위해 그대로 둔다.
28. **켤 때 지난 봉 이어 붙이기** (09-25) — `Hub._history` 가 받은 봉 앞에 DB 의 최근 `HISTORY_DAYS`(5) 날짜를 붙인다
    (`rp.get_bars(before=, days=)`). 국내는 KRX 정규장만, 오버나이트를 안 넣는 종목은 주간거래 봉을 뺀다. 차트를 며칠 뒤까지 보고
    RSI·MACD 를 긴 기록으로 셈한다. TSLA 120 → 1171봉(09-17~). 알림은 최근 값만 보니 영향이 거의 없다.
29. **시그널 방아쇠를 RSI 복귀로** (09-25, 사용자 결정) — 사용자: 「가격이 한참 떨어진 뒤에나 시그널이 나왔다」. TSLA 09-25: 07:35 꼭대기
    382.56·RSI 68.1, 옛 규칙(MACD 히스토그램 부호) 시그널은 08:10 381.50. DB 13종목 21,108봉으로 견줌(60분 뒤):
    MACD 부호 654개·늦음 30분·49%·+0.056 / RSI 복귀(극값 봉 뺌) 782개·10분·51%·+0.062 / **RSI 복귀(극값 봉도) 1012개·5분·50%·+0.048**
    / 히스토 돌아섬 1923개·5분(너무 많음) / MACD(6,13,5) 877개·15분·−0.002. 극값 봉을 빼면 TSLA 07:40(봉 안 69.9 찍고 63.3 으로
    닫힘)을 놓쳐서 극값 봉도 넣었다. 무장·강약·추세는 그대로. **10-02 채점 때 다시 생각하기로 함** (사용자).
30. **종목별 RSI 선 (틀만)** (09-25) — `al.Lines`(strong_lower, lower, upper, strong_upper), `Lines.parse`([25,75] 면 강한 선은 5 바깥).
    `level_of`·`edge_of`·`Gate(lines)`·`phrases`·`ks.signals(lines=)` 가 받는다. 서버 설정 `lines` {종목: [4개]}, `Hub.lines`, `set_lines`
    (알림 상태 새로, 시그널 다시 셈, 새 문장 준비), `POST /api/lines`. 줄마다 `lines` 를 보내 화면 색·막대·구간·차트 선이 따른다.
    「구간」 칸을 누르면 알림 줄에 칸 넷. 되감기 `--lines`, 없으면 설정의 종목 선. **값은 10-02 채점 보고 정한다** — 지금은 모두 기본.
    종목 선을 바꾸면 「25 미만」 같은 새 문장이 필요하다 — 로컬 TTS 로 녹음하려면 `tts_make.py` (Claude 가 돌린다).
31. **장이 없는 날 줄 줄이기** (09-25, 사용자 요청) — 그 종목 시장이 「휴장」(주말·휴일, 장 마감은 아님)이면 이름·가격·등락만 두고
    「휴장」, 나머지 칸은 비운다 (`marketClosed`, `cells`). 30초마다 줄을 다시 그려 장이 열리고 닫히는 것을 반영한다.
    서버도 `market_closed` 면 그 종목 선 알림·시그널을 안 본다 — 켤 때마다 「시작 때부터 🔇」 기록이 쌓였다. Gate 는 그대로 두어
    장이 열리면 처음 값이 「켤 때 이미 넘어 있음」처럼 소리 없이 시작한다.
32. **실제 알림 성적** (09-25) — `Hub.scores(symb)`(알림 기록 전체 + after 줄, 억제·켤 때 뺌), `GET /api/scores?symb=`,
    화면 「알림 기록」 옆 「성적」 표 (종류별 15·30·60분 건수·맞음(보합 뺌)·평균). 09-25 밤 기준 65 초과 45건 60분 맞음 44%,
    70 초과 19건 21%, 35 미만 13건 100% (한 날 치라 믿기 이르다).
33. **미국 조기 폐장** (09-25 밤) — `us_early_close(d)`: 7/3(월~목), 추수감사절 다음 금요일, 12/24(평일·휴장 아닐 때). 규칙으로 센다
    (`holidays` 에 없음, 2019·2021·2023~2025 실제 달력과 같음). 이날 `us_session` 은 정규장 13:00, 애프터 17:00 까지.
    서버가 `holidays.us_early` 로 보내고 화면은 「정규장 (13시 조기 마감)」, 17:00~20:00 은 「장 마감」. 다음은 2026-11-27.
34. **시작 인사 목소리 바꿈** (09-25 밤) — 자세한 것은 저장소 밖 `NOTES.local.md`.
35. **몇 분봉 RSI 가 잘 맞나** (`kis_tf.py`, 09-25 밤) — 1분봉 한 벌로 3~60분봉을 만들고 매분 진행 중 봉 RSI 를 Gate 에 넣어
    실시간처럼 울린다. 시그널은 닫힌 봉. 15·30·60·120분 뒤 채점, 앞 5거래일은 뺌, 주간거래 뺌.
    SOXL 20일(08-28~09-25) 미국 정규장 알림: 5분봉이 가장 나음 (15분 뒤 59%, 초과 +0.16%, 120분 뒤 +0.36%). 30분봉이 가장 나쁨.
    프리·애프터 넣으면 모든 봉이 50% 안팎.
36. **1분봉 쌓기** (09-25 밤) — `--collect` 가 모든 종목의 1분봉을 DB(nmin=1) 마지막 봉 뒤로 이어 받는다 (`topup_minutes`,
    `last_minute`). 주간거래 시간이면 주간거래 1분봉도 (BAQ 등, keyb 로 3쪽 ≈ 6시간 남짓만 온다 → 세션 중 수집 필요).
    1분봉은 약 25거래일만 거슬러 준다(TSLA 23,339개, 08-21~). 한 행 약 74바이트 → 종목당 1년 25MB 쯤.
    모니터링은 안 하고 1분봉만 쌓을 종목은 `kis_collect.json`(저장소 밖) — 09-25 에 18개(FNGU TQQQ MSFT AAPL META GOOG CBRS SPCX PLTR
    AMD AVGO INTC MRVL NVTS ORCL QCOM COIN IBIT)를 넣어 모두 31종목. 처음 수집 때 종목당 50초쯤 걸려 받는다. `kis_tf.py` 도 이 목록을 본다.

37. **채점 보고서 페이지** (`kis_report.py`, 09-25 밤, TODO 9) — `kis_tf.analyze` 로 모든 종목(수집 목록)을 채점해
    `replay_cache/report.html` 한 파일로. 서버 `/report` 가 그 파일을 준다 (서버 재시작 뒤). 시세가 들어 있어 저장소엔 안 올린다.
    봉 길이 표(정규장·모든 세션·미만·초과·시그널), 종목 × 봉 길이(60분 뒤), 5분봉 종류·세션별, 실제 알림(`Hub.scores`).
    맞음%가 50%에서 1/√n(2σ) 넘게 벗어난 칸만 굵게. 13종목 쌓인 것으로 40초쯤 (`--fetch` 면 이어 받은 뒤).
    Claude 가 볼 때는 미리보기 `report`(.claude/launch.json, 8766 에서 replay_cache 를 준다 — 8765 는 다른 것이 씀).
    **13종목 20일 첫 결과**: 정규장 알림은 모든 봉 길이에서 맞음 52~59%, 초과 +0.04~+0.14%(60분 뒤) — 약하지만 동전보다 낫다.
    30분봉이 60분 뒤 59%·+0.14 로 가장 좋고, 60분봉은 나쁘다. 5분봉은 54%·+0.07. 5분봉 30·35 미만이 60분 뒤 57~58%.
    세션: 미국 애프터 61%, 정규 55%, 프리 49%, 국내 46%(60분 뒤). SOXL 만 보면 5분봉이 낫다(35번).

38. **종목별 선 견주기** (09-25 밤, TODO 4 돕기) — `kis_tf.LINE_SETS`(35/65·30/70·25/75, 강한 선 5 바깥)마다 5분봉 알림을 걸어
    보고서 「종목별 선」 표(정규장, 60분 뒤, 하루 몇 번, ● 지금 선). 13종목 20일: 모두 합치면 35/65 54%·+0.07(하루 6.8),
    30/70 54%·+0.08(3.9), 25/75 56%·+0.09(1.8). 25/75 가 나은 종목 SOXL·KORU·BE·삼성전자, 30/70 은 DRAM·MU·GEV,
    35/65 그대로가 FCEL·LITE·POET. 건수가 적어(종목당 하루 1~2번) 10-02 한 달 치로 다시 본다.

39. **국내 넥스트레이드(통합 시세) 스위치** (09-26 새벽, 아직 실시간 확인 못 함) — 설정 `kr_market`: `"krx"`(기본, 지금과 같음) /
    `"unified"`. `k.set_kr_market` 이 `KR_TR`(H0STCNT0 / H0UNCNT0), `KR_CODE`(분봉 J / UN), `KR_OPEN`~`KR_CLOSE`(09:00~15:30 / 08:00~19:59)를
    같이 바꾼다. 서버는 켤 때 한 번(바꾸면 재시작), 되감기·수집·kis_tf·보고서는 `k.load_kr_market()` 으로 설정을 따른다.
    「⚙ 설정」의 「국내 넥스트레이드」 스위치(`/api/kr-market`, 상태 `kr_market: {want, now}`), 지수 띠에 「프리마켓 (NXT)」·「애프터마켓 (NXT)」.
    필드는 KIS 예제(open-trading-api `ccnl_total`)로 H0STCNT0 과 같은 46칸 차례임을 봤다. 09-23 삼성전자 분봉: J 는 15:30~20:00 봉이 섞여 오나
    08:00~08:50 은 없고, UN·NX 는 08:00~20:00 다 온다. 같은 분이라도 거래량이 J 1.9만·NX 7.7만·UN 9.7만(19:59)으로 다르다.
    DB 는 같은 `KRX` 이름에 쌓으니, 켜면 그 뒤로 09:00~15:30 봉도 통합 값으로 덮인다 (가격은 거의 같고 거래량만 다르다).

40. **백테스트 믿을 만한가 — 보고서에 넷 더** (09-26, 사용자가 조언 1~4 를 시킴). 기준은 13종목 20일, 5분봉 정규장 알림, 비용 0.2% 가정.
    - **앞·뒤 절반 나눠 확인** (가운데 날 09-10): 앞 절반은 모든 봉 길이가 초과 0 이하(−0.30~0.00), 뒤 절반만 + (+0.10~+0.31).
      → 「30분봉이 가장 좋다」는 뒤 절반 장세 덕이다. 종목마다 선·봉을 앞에서 골라도 뒤에서 종목의 절반 넘게 지금 것보다 못했다.
    - **몰린 알림 하나로 (같은 종목 60분 안)·날 단위 bootstrap 90% 구간**: 5분봉 1783→792건, 초과 +0.05, 구간 −0.05~+0.16 (0 을 걸침).
      모든 봉 길이가 0 을 걸친다 (30분봉 −0.00~+0.33 이 가장 가깝다). **아직 우연과 가를 수 없다.**
    - **비용 빼기·최대 역행**: 비용 0.2% 를 빼면 모든 봉 길이가 − (5분봉 60분 뒤 −0.15). 들어간 뒤 반대로 가는 폭의 평균이 60분에 −0.9% 쯤.
    - **변동폭(5분봉 14개 평균 진폭 %)**: 변동폭이 클수록 맞음·평균이 오른다 — 앞·뒤 절반 모두 (0.66% 위: 앞 60분 +0.12·120분 +0.33, 아래는 −0.04·+0.02).
      문턱 흉내: 앞 절반에선 어느 문턱도 비용을 넘지 못했고(0.8%·1% 가 0 근처), 뒤 절반은 0.4% 위부터 모두 +. **좋아지긴 하나 아직 비용을 확실히 넘진 않는다.**
    - **거래량(알림 앞 5분, 같은 세션 중앙값 대비)**: 얇은 때 성적이 부풀어 보이는 착시는 없었다 (칸마다 비슷).
    - 알림에 `atr`·`rvol`·`mae` 를 붙이는 것은 `kis_tf.scored`. 보고서 `--cost` 로 비용 가정을 바꾼다.

41. **다이버전스 백테스트** (09-26, 사용자가 붙인 「MACD RSI 다이버전스 승률 97%」 영상 요약에서) — `kis_diverge.py`:
    저점·고점 = 앞뒤 3봉보다 낮은(높은) 5분봉, 앞 점은 30봉 안(4봉 넘게 떨어짐), 가격 저점↓·RSI 저점↑ = 상승(고점은 반대).
    이중 = MACD 히스토그램도 어긋남, 전날 저가·고가 근처 = 변동폭 절반 안. 신호는 3봉 뒤 확인 봉 종가. `kis_tf.divergence_rows`,
    5분봉 알림에 `div`(앞 12봉 안 같은 쪽 다이버전스). 보고서 「다이버전스」 표.
    **13종목 20일 결과: 다이버전스는 기준보다 나빴다** — RSI 만 60분 맞음 43%·초과 −0.20(90% 구간 −0.33~−0.07, 0 아래),
    이중 38%·−0.28(−0.45~−0.14), 앞·뒤 절반 모두 −. 기준 값을 바꿔도(K 2·3·5, 앞 점 20·30·60봉) 모두 − (−0.08~−0.28).
    다이버전스 뒤에 난 알림도 다이버전스 없는 알림보다 못했다(−0.10 vs +0.07). 이 장세(반도체 급등)에선 다이버전스가 오히려
    추세가 이어지는 쪽이었다. 「이중 + 전날 선 근처」는 20일에 4번뿐. 한 달·다른 장세에서 다시 본다.
    **왜 나빴나 (정규장 다이버전스 451개, 60분 뒤)**: 저점 봉 종가에 들어갔다면(뒤 3봉을 봐야 저점인 줄 안다 — 나중에 차트를 보고 고르는 것)
    +0.22~+0.25%·62~66% 이지만, 확인 봉(3봉 뒤)에 들어가면 −0.13~−0.14%·45~46%. 그 3봉 사이에 이미 알림 쪽으로 +0.31% 움직였다
    (되돌림을 확인하느라 되돌림을 다 놓친다). 앞 30봉 동안 반대쪽으로 1.1~1.5% 온 센 흐름이라, 짧은 되돌림 뒤 흐름이 다시 이었다.
    영상의 「승률 97%」는 저점을 나중에 보고 고르는 착시일 가능성이 높다.

42. **이름난 RSI 로직 A·B·C** (09-26, 사용자가 검색 뒤 셋 다 넣으라 함) — 보고서 「이름난 RSI 로직」. 흐름 = 앞에 닫힌 60분봉 RSI ≥ 50.
    A 추세 필터(흐름 쪽 알림만), B 카드웰 선 옮기기(오름 35·40·80·85 / 내림 15·20·60·65, 오름·내림 선으로 따로 걸고 흐름 맞는 것만),
    C 코너스 RSI(2)(200봉 평균 위 RSI2<5 매수, 밑 >95 매도, 5봉 평균 넘으면 나옴, 포지션 중 새로 안 들어감). `kis_tf.trend_rsi`·`cardwell_rows`·`rsi2_rows`.
    13종목 20일 (5분봉 정규장, 몰린 것 하나로, 60분 뒤): 지금 55%·+0.05(앞 −0.01/뒤 +0.11).
    **A 57%·+0.12, 앞 +0.09/뒤 +0.15 — 처음으로 앞·뒤 절반 모두 +, 120분 비용 뺀 +0.09.** 다만 90% 구간 −0.04~+0.28(0 걸침),
    뺀 쪽(흐름 거스른 알림)도 +0.08 이라 가르는 힘은 아직 약하다. B 54%·+0.04 (지금과 같음). C 시간 기준 +0.02,
    나오는 규칙대로는 이긴 비율 62%·평균 +0.07·중앙값 +0.13 이나 가장 크게 잃은 한 번 −3.77% (작게 자주 벌고 가끔 크게 잃는 꼴), 비용 빼면 −0.13.
    출처: StockCharts RSI(2), MQL5 「Day Trading Larry Connors RSI2」(S&P500 30분봉에서 승률 50% 남짓), Wikipedia RSI(카드웰·Marek·Šedivá 2017).

43. **다이버전스 더 시험** (09-26, 전하가 제안 2·4·5 를 하라 함) — 보고서에 표 둘 더.
    - **히든 다이버전스**: `dv.find(hidden=True)` — 가격 저점↑·RSI 저점↓ = 상승 히든 (고점은 반대), `Divergence.hidden`. 이중(MACD)도 같은 식.
    - **긴 봉**: `kis_tf.DIV_TFS`(5·15·30·60분)마다 `divergence_rows(..., tf)`, 줄에 `trend`(60분 흐름)·`tf`. `info[종목]["div_tf"]`.
    - **손절·익절**: `kis_tf.bracket` — 손절 = 다이버전스 저점(고점) 봉 저가(고가), 익절 = 손절 폭 × 1.5·2(`BRACKET_R`),
      안 닿으면 390분(`BRACKET_MAX`) 뒤나 그날 마지막 1분봉. 한 1분봉에 둘 다 닿으면 손절. 보고서는 종목마다 거래 중 신호를 뺀다(`no_overlap`).
    - **13종목 20일 결과 (정규장, 몰린 것 하나로, 60분 뒤)**:
      5분봉 히든 56%·+0.09 (90% 구간 +0.01~+0.19 — 처음으로 0 위), 흐름 쪽 히든 +0.11 (+0.01~+0.22). 다만 앞 +0.20 / 뒤 +0.01~+0.03 으로
      뒤 절반에서 거의 0, 비용 빼면 60분 −0.08~−0.10. 15분봉 흐름 쪽 히든 +0.12(구간 0 걸침), 30·60분봉 히든은 −.
      보통 다이버전스는 5·15분봉 −0.20, 30·60분봉 +0.05(구간 0 걸침, 앞·뒤 들쭉날쭉).
      손절·익절대로: 5분봉 보통 이긴 비율 32~35%·평균 −0.23~−0.25, **5분봉 히든 1.5배 47%(본전 40%)·+0.02, 비용 빼면 −0.18**.
      15분봉 이상은 손절 폭이 1.8~2.8% 라 60~85% 가 시간 청산, 모두 비용 빼면 −. **비용을 넘는 다이버전스 규칙은 없다.**
    - 히든이 보통보다 나은 것은 41번(이 장세에선 흐름이 이어졌다)과 같은 이야기다. 뒤 절반에서 약해졌으니 새 데이터로 본다 (아래 사전 등록).

44. **사전 등록 판정 준비** (09-26) — `kis_report.py --since YYYYMMDD --set all|new|watch`.
    `--since` 는 그 전 날의 줄을 다 뺀다(`cut_since`, RSI 는 그 전 봉으로 셈한 그대로, 기준(bases)은 전체 기간 것). `--set new` 는 `kis_collect.json` 종목만(`pick_tickers`).
    보고서 맨 위에 「사전 등록 규칙」 표(`prereg_section`): R1(`kis_tf` `divergence_rows(reverse=True)` → `info["r1"]`),
    R2(5분봉 히든 중 흐름 쪽), R3(`dv.find(right=1)`, `R3_RIGHT` → `info["r3"]`), 참고로 보통 다이버전스·지금 알림.
    `--since` 가 `PREREG_SINCE`(20260928) 전이면 「판정용 아님」을 붙인다.
    판정 명령: `python kis_report.py --fetch --since 20260928` (그리고 `--set new`). 새 종목은 09-28 첫 수집 때 25거래일을 거슬러 받으니
    `--set new` 는 `--since` 없이도 안 본 데이터다 (앞 5거래일은 자리 잡기로 빠짐).
    20일 치 참고(판정 아님): R1 +0.20(비용 뺀 60분 +0.01), R2 +0.11, R3 −0.09. 09-15 부터만 자르면 R1 +0.03, R2 +0.01, R3 +0.09 — 흔들림이 크다.

45. **자동 시작 확인** (09-26 01:18, 전하 허락) — 전하가 터미널로 띄운 서버(`python kis_web.py`)를 내리고 `Start-ScheduledTask -TaskName "KIS RSI 모니터"`
    → pythonw 서버가 12초 만에 `/api/state` 200, 실시간 구독 모두 성공. **지금 서버는 이 pythonw 로 돈다** (창 없음, 기록은 `kis_web.log`).
    끄려면 작업 관리자에서 pythonw, 또는 `Get-CimInstance Win32_Process | ? CommandLine -like '*kis_web.py*'` 로 찾아 `Stop-Process`.
    같은 때 다시 켠 서버에서 33·37·39번(조기 폐장 `us_early`, `/report` 200, `kr_market`)이 들어간 것을 API 로 봤다.
46. **알림 목소리 Edge 스위치** (09-26, 전하: 「장중에 울리는 TTS 는 edge TTS 로 잠시 바꿔줘」) — 설정 `tts_engine`: `"local"`(기본) / `"edge"`.
    `Voice.prefer` 가 앞세울 목소리. Edge 면 Edge 파일이 없을 때 먼저 만들고, 안 되면(인터넷 없음) 로컬 녹음을 쓴다 (`make(text, prefer)`, `_save`).
    `cached` 는 Edge 를 골랐으면 Edge 파일만 세서 켤 때·바꿀 때 없는 것을 뒤에서 만든다. **시작 인사는 늘 로컬 자리 파일**(`greet` 가 `prefer="local"`) — 녹음해 둔 인사 그대로.
    「⚙ 설정」의 「알림 목소리 Edge」 스위치(`POST /api/tts-engine`, 바로 적용), 상태 `tts_engine`. 09-26 01:30 에 `edge` 로 두고 서버를 작업 스케줄러로 다시 켬
    → 78문장 만듦, TTS 테스트 `engine: edge`. 전하가 TTS 테스트와 시작 인사 **둘 다 들렸다**고 하심 — pythonw 서버에서도 소리가 난다.
47. **로컬 TTS 감정 지시 모두 뺌** (09-26, 전하: 「qwen 목소리에 감정을 넣으니 별로」) — `TTS_LOCAL_EMOTION = ()`. 남아 있던 것은 「시작」이 든 문장의
    「기쁘고 활기찬 목소리로」 하나였고, 알림·시그널 문장은 원래 지시 없이 녹음돼 있었다 (Webull 판도 알림은 감정 없음). 전하가 Edge 로 바꾼 까닭이
    이것이었다. 뒤이어 알림 목소리를 바꿨다 (`NOTES.local.md`) — 설정 `tts_engine` 은 다시 `local`. 지시가 캐시 이름에 들어가 시작 인사 녹음 두 파일의 이름이 바뀌어서 새 이름으로 복사했다 (옛 파일도 남김, `NOTES.local.md`). **「잠시」라 하셨으니 되돌릴지 나중에 여쭐 것** (스위치를 끄면 된다).

## 사전 등록 — 새 데이터로만 판정할 규칙 (09-26 적음)

지난 20일 결과를 보고 떠올린 규칙이라 같은 데이터로 재면 좋게 나올 수밖에 없다. **새 18종목(09-28 첫 수집)과 모든 종목의 09-28 이후 날만**으로 판정한다.
잣대는 설계 결정의 「규칙을 바꾸는 근거」와 같다 — 5분봉 정규장, 몰린 것 하나로(60분), 60분 뒤 초과의 날 단위 90% 구간이 0 위, 비용 0.2% 뺀 60·120분 + .
규칙을 적은 뒤에는 값을 바꾸지 않는다 (바꾸면 그 뒤 데이터로 다시 센다). 보고서 `--since 20260928`·`--set new` 로 잰다 (44번).

- **R1 거꾸로 타기**: 5분봉 보통 다이버전스(RSI 만, 앞뒤 3봉, 앞 점 30봉) 확인 봉 종가에 **다이버전스 반대쪽**으로 들어간다 (상승 다이버전스면 판다).
  20일 치에선 −0.20 을 뒤집은 +0.20 이 될 뿐이라 재지 않았다.
- **R3 확인 늦음 줄이기**: 저점·고점을 **왼쪽 3봉·오른쪽 1봉**으로 정해(나머지는 같음) 1봉 뒤 확인 봉 종가에 다이버전스 쪽으로 들어간다.
- **R2 흐름 쪽 히든** (43번, 20일 치에서 +0.11 이었으나 뒤 절반 +0.03): 5분봉 히든 다이버전스 가운데 60분 흐름과 같은 쪽만, 확인 봉 종가에 들어간다.

## 채점 결과 (09-25 첫 번째, 미국 11종목, 09-14~09-25, 60분 뒤)

| 종류 | 건수 | 맞음 | 초과(%p) |
|---|---|---|---|
| 매수 강 | 118 | 61% | +0.26 |
| 30 미만 | 182 | 58% | +0.14 |
| 35 미만 | 303 | 58% | +0.05 |
| 65 초과 | 503 | 47% | +0.11 |
| 70 초과 | 345 | 46% | +0.05 |
| 매수 약 | 86 | 52% | −0.08 |
| 매도 강·약 | 336 | 47~49% | +0.06~0.08 |

세션별: 정규장 30 미만 70%·+0.49, 프리장 매수 강 75%·+0.48. 위쪽 알림은 방향이 안 맞는다(오르는 장). 반도체 종목이 많아 같이 움직이니
건수만큼 믿지 않는다.

국내 (09-25, SK하이닉스·삼성전자, 08-26~09-23, KRX 정규장만, 60분 뒤, 보합 뺀 맞음·초과): 30 미만 51%·−0.07,
35 미만 35%·−0.21, 65 초과 56%·+0.03, 70 초과 56%·+0.10, 매수 강 55%·+0.11, 매수 약 27%·−0.36, 매도 강 39%·−0.11.
국내는 알림·시그널 모두 값어치가 거의 없고 35 미만·매수 약·매도 강은 거꾸로다. 두 종목뿐이라 믿을 만하지 않다.

**10-02 에 한 달 치로 다시 보고 4번(종목별 설정)·규칙 변경을 정하기로 했다.**

## 확정한 설계 결정

- **각자 자기 PC·자기 키로. 인터넷에 띄우지 않는다** — 시세 재배포는 약관으로 막혀 있다 (README).
- **알림 판정은 서버에서** — 브라우저를 닫아도 울린다.
- **실시간 연결은 앱키당 하나**(`ALREADY IN USE`) → `KisInUse`. 종목 추가·삭제는 구독만 넣고 뺀다. 두 번째 서버는 켜지지 않게 막았다.
- **실시간 필드 맨 앞에 구독 키(RSYM)가 한 칸 더 온다.** `RBAQ…` 로 시작하면 주간거래 체결.
- **접근토큰은 `kis_token.json` 에 24시간.** 새 토큰은 1분에 한 번만.
- **시그널은 분봉 전체로 매번 다시 셈**, 진행 중인 봉은 뺀다. 봉 안 RSI 최저·최고는 저가·고가로.
- **억제된 알림은 까닭마다 한 번만 기록.**
- **차트 시간축은 현지 시각을 UTC 인 척**(`epoch()`). 알림 기록의 `bar` 도 이 값이다 (되감기와 대조할 때 `calendar.timegm` 을 써야 한다).
- **알림·시그널은 주 분봉(5분)으로만.** 여러 시간봉은 보기만.
- **오버나이트 봉 포함은 종목마다 Webull 을 따른다**(위 10번). 주간거래 체결이 와도 「정규」 종목은 가격·등락만 고친다.
- **국내는 KRX 정규장만(기본)** — 실시간과 분봉을 같은 시장으로. 09-26 부터 설정 `kr_market: "unified"` 면 둘 다 통합(39번).
- **RSI 정수는 버림** — 반올림하면 69.6 이 70 인데 구간은 「65 초과」라 어긋난다.
- **켤 때 이미 넘어 있는 알림은 소리 없이** — 다시 켤 때마다 몰려 울렸다.
- **여러 시간봉은 뒤에서 받는다** — 켤 때 30초쯤 더 걸려서. 국내 60분봉은 1분봉을 묶느라 종목당 10초 남짓.
- **로컬 TTS 는 녹음할 때만** (VRAM 8GB). 종목을 더했거나 `TTS_SAY_AS` 를 바꾸면 Claude 가 `tts_make.py` 를 돌린다.
  읽는 법을 바꾸면 웹 서버도 다시 켜야 새 문장을 읽는다.
- **미국 휴장일은 `holidays` 꾸러미로** (09-25 사용자 결정) — 한국투자증권 해외결제일자조회(CTOS5011R)는 미국이 쉬는 날 목록에서 빠지지만
  한 달 남짓만 채워져 있다(담당자가 넣는 듯). 인베스팅닷컴 긁기는 Cloudflare·약관 때문에 안 한다. `exchange_calendars` 는 조기 폐장도 있지만 pandas 가 무겁다.
- **실시간 알림의 성적은 알림 기록에 쌓는다**(22·32번) — 되감기(흉내)와 따로, 진짜로 울린 알림의 15·30·60분 뒤.
- **분봉은 SQLite 한 파일**(`replay_cache/bars.db`) — gzip JSON·달별 파일보다 동시 쓰기에 안전하고 새 봉만 넣는다(26번). 서버·수집·되감기가 같이 쓴다.
- **시그널 방아쇠는 RSI 복귀(극값 봉 포함)** — 10-02 에 다시 본다(29번).
- **장이 없는 날은 알림·시그널을 안 본다**(31번). 기준은 화면 「휴장」과 같다 (장 마감은 아님).
- **README 화면 사진은 바꿀 때마다 새 이름** `docs/screen_YYYY-MM-DD.png`, 같은 날 또 바꾸면 `_2` 를 붙인다 (같은 이름은 깃허브 캐시가 옛 것을 보여 준다).
- **예약 실행은 윈도우 작업 스케줄러로** (Claude 예약 작업은 한 번에 세션 하나라 30분마다 돌리기엔 낭비). 창이 뜨지 않게 `.vbs` 또는 `pythonw`.
- **설정 화면**: 스위치는 ON/OFF 토글, 설명은 ⓘ (PC 는 마우스 올리기, 폰은 눌러 펼치기 — `title` 은 폰에서 안 보인다).
- **화면**: 글자 기본 10px, 한국식 색(상승 빨강·하락 파랑), 깜박임 없음. 제목 「<작게>한량이 만든</작게> 한국투자증권 RSI Monitor ver 0.2」.
- **문서는 쉬운 평서체**, 숫자를 넣는다. 커밋 메일은 `13573570+destory1984@users.noreply.github.com`.
- **백테스트는 1분봉 한 벌에서** (`kis_tf.py`, 35번) — 모든 봉 길이를 같은 1분봉으로 만들고, 매분 「진행 중인 봉 RSI」를 Gate 에 넣는다 (실시간과 같게).
  주간거래 봉은 뺀다(1분봉은 지금 세션 것만 받아져서). 앞 5거래일은 RSI 자리 잡기로 뺀다.
- **규칙을 바꾸는 근거의 잣대** (40번) — 칸 하나가 좋다고 바꾸지 않는다. ① 앞 절반에서 고른 것이 뒤 절반에서도 좋은가
  ② 같은 종목 60분 안에 몰린 알림은 하나로 세고, 날 단위로 다시 뽑은(bootstrap) 90% 구간이 0 위인가 ③ 비용(0.2% 가정, `--cost`)을 빼도 + 인가
  ④ 최대 역행이 버틸 만한가. 결과를 보고 뒤집은 규칙은 **아직 안 본 데이터**(새 18종목, 09-28 이후)로만 판정한다.
- **서버 알림 규칙은 10-02 한 달 치 뒤 사용자가 정한다** — 그전엔 보고서에만 넣는다 (A·변동폭 문턱·다이버전스 모두 서버엔 없다).
- **1분봉은 모든 종목 이어 쌓기** (36번) — 한국투자증권이 한 달쯤만 거슬러 주니 DB 가 유일한 긴 기록. 모니터링 안 하는 종목은 `kis_collect.json`.
- **국내 시장 스위치 `kr_market`** (39번) — 기본 `krx`. `unified` 는 실시간(H0UNCNT0)·분봉(UN)·시각(08:00~20:00)을 같이 바꾼다 (섞이면 RSI 가 어긋난다).
- **채점 보고서는 로컬 파일** (`replay_cache/report.html`, 서버 `/report`) — 시세가 들어 있어 저장소·인터넷에 올리지 않는다 (Artifact 도 안 씀).

## 시도했다가 버린 것

- **주간거래 체결은 가격만 고치고 RSI 에는 안 넣기** — MU 가 Webull 과 크게 달랐다(34.70 vs 72.20). → 넣기로.
- **모든 종목에 오버나이트 봉 넣기** — FCEL 이 Webull 과 달랐다(69 vs 51.92). → 종목마다(10번).
- **미국 가격 늘 소수 둘째** — 사용자 뜻을 잘못 알아들었다. 1달러 밑은 넷째가 맞다. (그 커밋은 push 전에 지웠다.)
- **Claude 예약 작업으로 30분마다 수집** — 윈도우 작업 스케줄러로.
- **작업 스케줄러에서 bash 를 바로 띄우기** — 창이 뜬다 → `collect_day.vbs` 로 숨김.
- **pythonw 로 띄울 때 `sys.stdout.reconfigure`** — stdout 이 None 이라 죽었다 → None 이면 건너뛴다.
- **로컬 TTS 를 늘 켜 두기** — 사용자가 싫다고 했다.
- **`qwen-tts` 만 깔기** — PyPI 최신 `torchaudio`(2.11)가 딸려 와 `torch` 2.6 과 안 맞아 `WinError 127`. → `torch torchaudio` 를 cu124 에서 같이.
- **DRM3** — 한국투자증권 해외 시세에 없다(유럽 상장). **다우존스** — 코드 못 찾음. **코스닥** — 사용자가 빼라 함.
- **한국투자증권 로고**, **여러 사람에게 보여 주는 서버**, **값 바뀔 때 깜박임**, **Edge +30%** (빠름).
- **시그널 방아쇠 「MACD 히스토그램 부호」** — 꼭대기에서 30분 늦다 (29번). 「히스토그램 돌아섬」은 시그널 3배라 시끄럽고,
  MACD(6,13,5)는 초과 −0.002 로 나빴다. 「RSI 복귀, 극값 봉 빼기」는 TSLA 07:40 같은 봉(치솟았다 바로 꺾임)을 놓쳤다.
- **되감기에서 넥스트레이드를 「NXT」 세션으로 따로 세기** — 서버 실시간이 KRX 만이라 뺐다 (21번).
- **분봉 캐시 JSON 파일** — 수집이 파일 통째를 다시 써 기간이 길수록 느리고, 수집·되감기가 겹치면 한쪽이 쓴 게 사라진다. gzip·달별 파일도 견줬다 → SQLite.
- **DB 에 쓰다 말고 네트워크 기다리기** — 잠금 30초 걸림을 시험에서 봤다 → 다 받은 뒤 한 번에 쓰고 commit.
- **종목 줄마다 「35/65」 선 표시** — 어지러워서 바꾼 종목만 「25/75」 표시, 「구간」 칸 전체를 누르게.
- **미국 휴장일을 손으로 적기 / 해외결제일자조회(CTOS5011R) / 인베스팅닷컴 / exchange_calendars** — `holidays` 로 (설계 결정 참고).
- **장 없는 날 켤 때마다 「시작 때부터 🔇」 기록** — 쌓여서 거슬린다 → 31번.
- **「30분봉이 가장 좋다」로 봉 길이 바꾸기** — 13종목 합계로는 30분봉이 60분 뒤 59%·+0.14 였지만, 앞 절반에선 −0.07 이었다 (뒤 절반 장세 덕). SOXL 만 보면 30분봉이 가장 나빴다.
- **종목마다 선·봉 길이를 골라 맞추기** — 앞 절반에서 가장 좋던 것을 골라도 뒤 절반에서 종목 절반 넘게 지금 것보다 못했다 (과최적화). 38·40번.
- **다이버전스를 신호로** (41번) — RSI 만 −0.20%(90% 구간 −0.33~−0.07), 이중 −0.28%, 기준 값 다섯 가지 모두 −. 까닭: 저점은 3봉 뒤에야 알 수 있는데
  그 사이 되돌림(+0.31%)을 다 놓친다, 센 흐름 한가운데라 흐름이 다시 이어진다. 「승률 97%」 영상은 저점을 나중에 보고 고르는 착시로 본다.
- **다이버전스 긴 봉(15·30·60분)·손절익절(1.5·2배)** (43번) — 모두 비용을 넘지 못했다. 긴 봉은 손절 폭이 커 대부분 시간 청산.
- **B 카드웰 선 옮기기·C 코너스 RSI(2)** (42번) — 지금 알림보다 낫지 않았다. RSI(2) 는 이긴 비율 62% 지만 한 번에 −3.77% 잃는 꼴, 비용 빼면 −.
- **날짜 풀기를 `strptime` 으로** — 채점 시간의 90% 넘게 먹었다(종목당 500만 번) → `rp.ts` 를 글자 자르기로 (57초 → 7초).
- **1분봉을 늘 25일 치 새로 받기** — 종목당 200번 부르기 → DB 마지막 봉 뒤로만 이어 받기(`topup_minutes`, 종목당 한두 번).
- **거래량 비율을 모든 분의 중앙값으로** — 프리장이 「보통」에 섞였다 → 같은 세션끼리의 중앙값.
- **`kis_diverge.pivots(k=PIVOT_K)` 처럼 모듈 값을 기본 인자로** — 정의할 때 굳어 값을 바꾸면 확인 봉이 범위를 넘었다 → 부를 때 읽는다.
- **Claude 가 `git filter-branch`·강제 push 를 돌리기** — 자동 권한 검사가 「되돌릴 수 없는 git 작업」으로 막는다 (채팅 허락으로 안 풀림).
  → 스크립트를 만들어 사용자가 직접 돌렸다. 설정 규칙을 Claude 가 바꿔 우회하지 않는다.
- **devin MCP** — 403 으로 쓰이지도 않으면서 컨텍스트 3만 토큰 → 지웠다 (백업 있음).

## 아직 확인 못 한 것

- **국내 실시간 체결** — 연휴라 못 봤다. **2026-09-28(월) 장중**에 삼성전자·SK하이닉스 체결, H0STCNT0 필드가 맞는지.
  국내 분봉은 KRX 정규장만 쓰니(21번) 15:30 뒤 값은 멈춘다. 켜 둔 서버와 새로 켠 서버의 국내 RSI 가 같은지도 본다.
- **국내 넥스트레이드 통합 실시간(H0UNCNT0)** (39번) — 스위치를 켜고 서버를 다시 켠 뒤 체결이 오는지, 08:00 프리·15:30~20:00 애프터에도 오는지.
- **다시 켠 서버의 새 것들** — 조기 폐장 표시(33번, 다음은 11-27), `/report`(37번), 설정의 「국내 넥스트레이드」 스위치 누르기(39번).
- **새 18종목 첫 1분봉 수집** — 09-28 09:00 `collect.log` 에 `+` 가 찍히는지, `kis_report.py` 가 31종목을 다 채점하는지.
- **알림 시간대·켤 때 소리 막기·여러 시간봉·켤 때 등락** — 시험은 가짜 데이터·REST 로만. 사용자 서버에 들어가 있으니 화면·기록으로 본다.
- **종목만 소리 끄기(19번)로 끈 종목에 실제 알림이 났을 때** 소리 없이 「🔇 종목 소리 끔」으로 남는지. **종목별 선(30번)을 실제로 바꾼 경우** (가짜 데이터로만 시험).
- **텔레그램으로 실제 알림이 가는지** (시험 메시지만 보냄). **새 시그널 규칙(29번)이 실시간 봉 경계에서 제때 울리는지.**
- **봉 경계에서 실제 시그널 알림**, **`kis_rsi.py setup`**(입력을 받아서 못 돌림).
- **주간거래 분봉은 「가장 최근 세션」을 준다** — 09-25 뉴욕 08:42(세션 끝나고 4시간 40분)에도 어젯밤 20:00~03:55 를 통째로 줬다
  (SOXL 96/96, TSLA 94). **다음 세션이 열리기 직전까지 받아지는지** (예: 목요일 밤 세션을 월요일 08:59 에) → 09-28 08:30 예약 작업이 본다.
  맞으면 30분 수집을 줄일지 사용자에게 묻되, **주간거래 1분봉은 세션 중 6시간 남짓만 받아지니(36번) 줄이면 1분봉 밤 봉에 구멍이 난다**고 같이 말한다.

## 다음 단계

1. 서버는 09-26 01:30 에 pythonw(작업 스케줄러)로 다시 켰다 (45·46번). 알림 목소리 Edge 를 언제 되돌릴지 여쭌다.
2. **09-28(월) 08:30 예약 작업** `kis-night-bars-check`: 주간거래 분봉 확인 → 수집 시간 줄일지 사용자에게 (위 1분봉 밤 봉 주의).
3. **09-28(월) 09:00 이후**: 새 18종목 1분봉이 들어왔는지 `replay_cache/collect.log`.
4. **09-28(월) 장중**: 위 「아직 확인 못 한 것」 — 국내 실시간, 넥스트레이드 스위치, 자동 시작, 알림 시간대, 종목 소리 끄기.
   README 사진을 장중 화면(차트 포함)으로 바꿀지 사용자에게.
5. **사전 등록 규칙 R1·R2·R3 판정** — 10-02 에 `python kis_report.py --fetch --since 20260928` 와 `--set new` 로 (44번). 한 주 치라 건수가 적으면 더 기다린다.
6. **10-02(금) 17:30 예약 작업** `kis-replay-review`: 한 달 치 보고서 → A 추세 필터·변동폭 문턱(둘을 겹친 것도)이 버티는지, 시그널 방아쇠·종목별 선(TODO 4).
   버티면 서버 알림에 **꺼 둔 스위치**로 넣을지 사용자에게 묻는다 (문턱 밑·흐름 거스른 알림은 소리 없이 기록만).
7. 그 뒤 후보: 수집 시간 줄이기.

## 파일

| 파일 | 하는 일 |
|---|---|
| `kis_rsi.py` | 키(`load_keys`: 환경변수 → `kis_config.json` → `~/.bashrc`)·토큰, 분봉(미국 `fetch_bars`(keyb 거슬러), 국내 `fetch_kr_bars`(`KR_OPEN`~`KR_CLOSE`), 주간 `fetch_night`, `merge_bars`), 국내 시장 `KR_MARKETS`/`set_kr_market`/`load_kr_market`/`kr_minutes`(`KR_TR`·`KR_CODE`), 현재가 `fetch_quote`, 지수 `MARKETS`, 세션 `us_day_session`/`us_session`/`us_closed`(`holidays.NYSE`)/`us_early_close`/`us_early_closes`, 국내 휴장 `kr_holidays`, 실시간 `live`/`subscribe`(`KR_TRS`), RSI·MACD, `Book`, `px`, 터미널 명령 |
| `kis_web.py` | FastAPI 서버 `Hub`: 종목(`_add`, `_bars`, `_history`, `store`/`store_closed`, `_tf_books`, `load_tf`, `reorder`, `set_night`), 실시간(`kis_loop`, `session_loop`), 알림·시그널(`check_alerts`, `check_signals`, `muted`, `market_closed`, `lines`/`set_lines`, `set_mute`, `fill_after`, `scores`), 지수 띠·휴장일(`load_holidays`), `row`/`mtf`/`state`. API `/api/state` `/api/chart/{symb}` `/api/tickers` `/api/order` `/api/night` `/api/mute` `/api/lines` `/api/scores` `/api/telegram` `/api/telegram/test` `/api/sound` `/api/signal-sound` `/api/sound-when` `/api/sound/test` `/api/kr-market` `/report` `/ws`. `state` 에 `kr_market: {want, now}`, `holidays.us_early`. 옵션 `--log`, 중복 막기 |
| `kis_alert.py` | `Lines`/`DEFAULT_LINES`, `Gate(lines)`(선·재무장·쿨다운, `check(v, now)`), `level_of`/`edge_of`, 읽는 법 `TTS_SAY_AS`, 문장 `say_breach`/`say_signal`/`phrases`, `Voice`(캐시 `path`, 로컬/Edge/SAPI, `greet`), `trim_wav` |
| `kis_signal.py` | 매수·매도 시그널 (`rsi_band`, `signals(lines=)` — RSI 복귀 방아쇠) |
| `kis_replay.py` | 알림 채점(되감기, 미국·국내, `--lines`, `lines_for`, `session`(조기 폐장 앎), `ts`(빠른 날짜 풀기), `score`/`exit_index`/`ret`) + `--collect`(모든 종목 1분봉 `topup_minutes`/`last_minute`(`MIN_DAYS`=25) + 주간거래 5분봉·1분봉, 종목 `collect_tickers` = 목록 + `kis_collect.json`). 분봉은 `replay_cache/bars.db`(`db`, `put_bars`, `get_bars(before=, days=)`, `get_night`/`set_night`, `migrate`) |
| `kis_tf.py` | 1분봉 백테스트: `load_minutes`, `aggregate`, `walk_alerts`(매분 진행 중 봉 RSI → Gate), `walk_signals`, `scored`(+ `atr_pct`·`rel_volume`·`adverse`), `run_ticker`/`analyze`(봉 길이·선(`LINE_SETS`)·다이버전스(`DIV_TFS` 마다 `divergence_rows`(히든 포함, 손절익절 `bracket`)/`tag_after_divergence`)·A·B·C(`trend_rsi`/`cardwell_rows`/`rsi2_rows`)), `stats`, 터미널 표 (`--tf --by 종목 --detail --offline`) |
| `kis_report.py` | 채점 보고서 → `replay_cache/report.html` (`build(offline, cost)`, `--fetch --open --cost`). 표: 봉 길이, 나눠서 확인, 몰린 알림 하나로(`dedupe`/`day_band`), 비용·역행, 다이버전스(히든·봉 길이별 `div_tf_section`·손절익절 `bracket_table`/`no_overlap`), 이름난 로직(`famous_section`/`verdict_table`/`rule_table`), 변동폭(`atr_table`/`gate_table`), 거래량, 종목×봉, 종목별 선, 5분봉 종류·세션, 실제 알림 |
| `kis_diverge.py` | 다이버전스 찾기 (`pivots`, `prev_day_levels`, `find(hidden=)` → `Divergence(confirm, pivot, prev, up, macd, level, hidden)`, `PIVOT_K`·`LOOKBACK`·`MIN_GAP`) |
| `static/index.html` | 웹 화면 한 파일 (lightweight-charts 4.2.3, CDN) |
| `tts_server.py` / `tts_make.py` | 로컬 Qwen3-TTS 서버 / 그것을 잠깐 띄워 녹음하고 내리기 |
| `kis_telegram.py` | 텔레그램 보내기 (`Telegram`, `alert_text`/`signal_text`, `setup`/`test` 명령) |
| `tests/test_rules.py` | 규칙 시험 (`python -m unittest discover tests`) |
| `collect_day.vbs` | 작업 스케줄러가 창 없이 `--collect` 를 돌리게 (Git Bash 경유) |
| `README.md`, `TODO.md`, `NOTES.md`, `requirements.txt`, `docs/screen_2026-09-25_2.png` | |

저장소에 안 올라가는 것(`.gitignore`): `kis_config.json`, `kis_token.json`, `kis_exchange.json`, `kis_watchlist.json`(종목 목록), `kis_collect.json`(1분봉만 쌓을 종목),
`kis_settings.json`(소리·인사·`night`·`sound_sessions`·`quiet`·`mute`·`lines`·`telegram`·`kr_market`), `kis_alerts.jsonl`(알림 기록 + after 줄), `kis_holidays.json`,
`NOTES.local.md`(저장소에 안 올리는 노트 — 시작 인사 목소리), `tts_cache/`(+ `backup_sohee/`), `replay_cache/`(`bars.db`(5분봉 nmin=5, 1분봉 nmin=1), `collect.log`, `json_backup/`, `report.html`),
`kis_web.log*`, `.venv*/`(로컬 TTS 는 `.venv_tts`), `.claude/`. 저장소 폴더의 `start` 파일은 사용자 것이라 건드리지 않는다.

## 시험할 때

- **규칙을 고치면 `python -m unittest discover tests` 부터** (73개, 네트워크·실제 파일 안 씀). 새 규칙은 `tests/test_rules.py` 에 시험도 더한다.
  `Gate` 시험은 시각을 `N`(10억 초)부터 준다 — 첫 쿨다운이 0 초부터 세어진다.
- 키: `source ~/.bashrc` 하거나, 이제는 `load_keys` 가 `.bashrc` 를 직접 읽는다. 키 값은 화면에 찍지 않는다.
- Git Bash 에서 한글: `PYTHONIOENCODING=utf-8`.
- **사용자 서버를 끊지 않게**: REST 조회(분봉·현재가·되감기·수집)는 괜찮다. 실시간(`live`/`rsi`/`watch`/새 `kis_web.py`)은 안 된다.
  서버 쪽 코드는 `Hub.__new__` 로 빈 Hub 를 만들어 필요한 속성만 채워 시험한다. `kis_web.ALERT_LOG`·`SETTINGS` 는 임시 파일로 바꾼다.
- 화면은 사용자 서버(localhost:8000)가 `static/index.html` 을 디스크에서 바로 주니, 브라우저 창에서 새로고침하고 JS 로 가짜 값을 넣어 본다.
  (Edit 뒤 훅이 `file://` 탭을 새로 열 수 있다 — localhost 탭으로 돌아갈 것.) 화면 좌표는 CSS 픽셀 × devicePixelRatio(1.25).
- **긴 heredoc 은 셸이 「unexpected EOF」로 깨지곤 한다** → 고칠 내용을 스크래치패드에 `.py` 로 쓰고 돌린다. 경로의 `\r`(`C:\_c\rsi`)도 heredoc 에서 줄바꿈이 된다.
- **heredoc 안 파이썬 문자열의 `\n`, `\2`, `\s` 가 망가진다.** 정규식·이스케이프가 든 줄은 Edit 도구로 고치거나, 스크립트 파일에 `chr(92)` 로 쓴다.
  고친 뒤 `node --check`(index.html 의 스크립트 부분) / `ast.parse` 로 확인한다.
- 소리는 Claude 가 못 듣는다. 만들었다·틀었다까지만 말하고 들렸는지는 사용자에게 묻는다.
- **보고서 볼 때**: `python kis_report.py`(쌓인 1분봉, 13종목 45초쯤) → 미리보기 `report`(`.claude/launch.json`, 8766 에서 `replay_cache` 를 준다.
  8765 는 다른 것이 쓴다) → `http://localhost:8766/report.html`. 표 숫자는 HTML 을 정규식으로 벗겨 읽는 게 빠르다. 다 보면 미리보기를 끈다.
- 스크래치패드는 `$TMP` 가 아니다 — `C:/Users/eofeo/AppData/Local/Temp/claude/C---c-koreainvest/<세션>/scratchpad` 를 그대로 적는다.
  파이썬에서 한글을 찍을 땐 `PYTHONIOENCODING=utf-8` (없으면 cp949 로 죽는다).
