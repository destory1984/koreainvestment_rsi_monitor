# 개발 노트

> 2026-09-25 기준. 새 세션이 이 파일만 읽고 이어서 작업할 수 있게 쓴다.
> 할 일 목록은 [TODO.md](TODO.md), 쓰는 법은 [README.md](README.md) 에 있다.

## 지금 상태

- 저장소: https://github.com/destory1984/koreainvestment_rsi_monitor (공개, 옛 이름 `koreainvestment_us_rsi` 는 넘겨 준다)
- 로컬: `C:\_c\koreainvest`, 브랜치 `main`
- 커밋은 기능마다 하고, push 는 가끔 한꺼번에 한다.
- 웹 서버가 이 세션의 백그라운드로 떠 있을 수 있다 (`python kis_web.py --no-browser`, 포트 8000).
  세션을 지우면 같이 내려갈 수 있으니 필요하면 다시 띄운다.
- 로컬 TTS 서버(Qwen3-TTS, 포트 47650)는 `C:\_c\tts` 쪽에서 따로 띄운 것이다. 이 저장소가 띄우지 않는다.

## 지금까지 한 것

1. **분봉·실시간 받기** (`kis_rsi.py`) — 미국 5분봉(HHDFS76950200, 한 번에 120개), 미국 실시간 체결(HDFSCNT0).
   국내는 1분봉(FHKST03010230, 한 번에 120개)을 거슬러 받아 5분봉으로 묶고, 실시간은 H0STCNT0.
2. **RSI·MACD** — 와일더 RSI(14), MACD(12,26,9). 진행 중인 봉은 체결마다 종가를 바꿔 다시 계산.
   2026-09-24 장중 Webull 화면과 같은 순간 견줘 66건 중 차이 중앙값 0.18 (README 에 표).
3. **터미널 명령** — `bars` / `live` / `rsi` / `watch` / `setup`.
4. **웹 화면** (`kis_web.py` + `static/index.html`) — FastAPI 서버 하나가 실시간 연결을 잡고 브라우저에 웹소켓으로 0.5초마다 바뀐 종목만 보낸다.
   종목 표, 5분봉·RSI·MACD 차트 셋(시간축 맞춤), 종목 추가·삭제(휴지통), 지수 띠, 알림 기록 세 줄.
5. **소리 알림** (`kis_alert.py`) — webull_rsi_monitor 와 같은 규칙: 35/65 바깥선(땡 + 말), 30/70 강한 선(세 음 + 말),
   재무장 7, 쿨다운 10분. 서버가 판정하고 서버 PC 스피커로 낸다.
6. **목소리** — 로컬 TTS(Qwen3-TTS 1.7B, Sohee) → Edge 음성(ko-KR-SunHiNeural, +20%) → 윈도우 SAPI 순서로 고른다.
   캐시 파일 이름이 webull_rsi_monitor 와 같은 규칙이라 그쪽 `tts_cache` 를 같이 쓸 수 있다.
7. **알림 기록** — `kis_alerts.jsonl` 에 한 줄씩, 서버를 다시 켜도 남는다. 표의 「구간」·「최근 알림」 칸, 차트의 알림 봉 표시.
8. **지수 띠** — S&P500(SPX) · 나스닥 종합(COMP) · 코스피(0001) · 니케이(JP#NI225) · 원/달러(FX@KRW)는
   한국투자증권 REST 를 10초마다, 비트코인은 업비트 공개 시세(KRW-BTC). 코스닥(1001)은 2026-09-25 에 뺐다.
9. **매수·매도 시그널** (`kis_signal.py`) — webull_rsi_monitor 의 rsi_signal.py 규칙. 09-24 Webull 시그널 15개 중 13개가 같은 봉.
10. **남이 쓰기 쉽게** — `python kis_rsi.py setup` 으로 키 넣기, `requirements.txt`, 켜면 브라우저 열기, `tts_server.py` 동봉.
11. **미국 주간거래(한국 낮)** — 미국 동부 20:00~04:00 에는 실시간을 `R`+`BAQ/BAY/BAA` 키로 바꿔 받고(30초마다 시각 확인),
    체결을 분봉·RSI·알림에 넣는다. Webull 5분봉도 오버나이트 봉을 넣어 RSI 를 낸다 (09-25 MU: 우리 34.70 vs Webull 72.20 으로 확인).
    켤 때 `BAQ` 분봉도 받는다 (09-25 확인, MU 봉이 20:00~02:40 빈틈 없이 옴).
    Webull 은 24시간 거래 종목만 오버나이트 봉을 넣는다 (09-25 FCEL: 오버나이트 빼면 51.95, Webull 51.92).
    그래서 최근 오버나이트 5분 칸 96개 중 절반 넘게 체결 있으면 넣는다. 09-25 새벽 기준 SOXL·MU·SNDK·KORU·TSLA·BE·DRAM·LITE 넣음,
    POET(31)·FCEL(6)·GEV(5) 뺌. 틀리면 화면의 「24h」「정규」 표시로 바꾸고 kis_settings.json 의 "night" 에 남는다.
12. **RSI 는 소수점 없이** 보인다. 반올림하면 69.6 이 70 인데 구간은 「65 초과」라 어긋나서 버림으로 한다.

## 확정한 설계 결정

- **각자 자기 PC·자기 키로 돌린다. 인터넷에 띄우지 않는다.** 시세 재배포(약관·거래소 계약), 앱키 하나에 실시간 연결 하나·41종목,
  소리가 서버 PC 에서만 난다는 세 까닭. README 「인터넷에 띄우지 않는 이유」에 적었다.
- **알림 판정은 서버에서 한다.** 브라우저를 닫아도 울려야 한다. 브라우저는 보여 주기만.
- **실시간 연결은 앱키 하나에 하나뿐**(`ALREADY IN USE appkey`). 웹 서버를 켜 둔 동안 `kis_rsi.py live/rsi/watch` 를 따로 못 켠다.
  → `KisInUse` 예외로 알아보기 쉽게 알린다. 종목 추가·삭제는 연결을 끊지 않고 구독만 넣고 뺀다(tr_type 1/2).
- **실시간 필드 맨 앞에 구독 키(RSYM)가 한 칸 더 온다.** 공식 예제 필드 목록에 없다. 국내(H0STCNT0)는 칸 수로 알아서 판단한다(`skip`).
- **접근토큰은 파일(`kis_token.json`)에 두고 24시간 쓴다.** 새 토큰은 1분에 한 번만 받을 수 있다.
- **시그널은 분봉 전체로 매번 다시 셈한다.** 상태 저장·예열이 필요 없다. 진행 중인 봉은 넣지 않는다(헛크로스 방지).
  봉 안 RSI 최저·최고는 저가·고가를 종가 자리에 넣어 계산한다(마지막 봉 RSI 는 종가에 대해 단조 증가).
- **억제된 알림은 까닭(재무장/쿨다운)마다 한 번만 기록한다.** 0.5초 판정이라 선 언저리에서 쏟아진다.
- **차트 시간축은 현지 시각을 UTC 인 척 넣는다**(`epoch()`). 미국은 미국 동부, 국내는 한국 시각으로 찍힌다.
- **세 차트의 봉 수를 같게** 하려고 값이 없는 봉도 `{time}` 만 넣는다. 안 그러면 스크롤 맞춤이 어긋난다.
- **캐시 열쇠에는 목소리를 바꾸는 설정을 모두 넣는다**(화자·시드·감정·자르기 / Edge 목소리·빠르기·음높이·크기).
- **시작 인사**: 기본은 「모니터링을 시작합니다」. 사용자 PC 는 `kis_settings.json` 의 greeting 으로 따로 둔다(저장소에 안 올라감).
- **화면**: 글자 기본 10px, 한국식 색(상승 빨강·하락 파랑), 깜박임 없음, 종목 칸은 'SK하이닉스' 폭(74px).
  제목은 「<작게>한량이 만든</작게> 한국투자증권 RSI Monitor ver 0.2」.
- **문서는 쉬운 평서체**(~다), 숫자를 넣는다. README 첫머리에 "한국투자증권과 관계없는 개인 프로젝트" 문구.
- **커밋 주소는 가림 주소만**: `13573570+destory1984@users.noreply.github.com` (저장소 설정에도 박아 둠). push 뒤 깃허브 API 로 확인.

## 시도했다가 버린 것

- **DRM3 추가** — 런던(LSE)·독일(Xetra) 상장 ETP 라 한국투자증권 해외 시세(미국·홍콩·중국·일본·베트남)에 없다. 기초 자산인 DRAM 으로 본다.
- **다우존스 지수** — `.DJI`, `DJI`, `INDU`, `$DJI`, `DJI@DJI`, `.DJIA`, `DJIA` 모두 빈 값. 코드를 못 찾아 뺐다.
- **한국투자증권 로고** — 상표라 공개 저장소에 넣지 않는다. 오해를 막는 문구만 README 에 둔다.
- **시세를 여러 사람에게 보여 주는 서버** — 위 설계 결정 참고.
- **README 목소리 표의 「윈도우 음성 — 인터넷이 끊겼을 때」 줄** — 인터넷이 끊기면 시세도 못 받으니 뜻이 없다. 코드의 SAPI 물러나기는 Edge 가 막힐 때를 위해 남겼다.
- **Edge 음성 +30%** — 빠르다고 해서 +20% 로 내렸다.
- **같은 이름으로 README 화면 사진 덮어쓰기** — 깃허브·브라우저가 옛 사진을 계속 보여 준다. 바꿀 때는 이름을 새로 짓는다(지금 `docs/screen_2026-09-25.png`).
- **값이 바뀔 때 칸 깜박임** — 사용자가 없애라고 했다.

## 아직 확인 못 한 것

- **국내 실시간 체결** — 추석 연휴(09-24~26)라 못 봤다. 2026-09-28(월) 장중에 삼성전자(005930)·SK하이닉스(000660) 체결이 들어오는지,
  H0STCNT0 필드가 밀리지 않는지 볼 것. 구독은 성공했다.
- **국내 분봉의 15:30 이후** — 분봉에는 넥스트레이드 애프터(~20:00)가 들어오는데 실시간은 KRX 만 받는다. 15:30 이후 값이 멈춘다.
- **봉이 넘어갈 때 새 봉 붙이기** — 코드는 있고 시그널 흉내 시험은 했으나, 실제 장중 경계를 눈으로 본 적은 없다.
- **장중 실제 시그널 알림** — 흉내 시험만 했다.
- **Edge mp3 실제 재생** — 사용자가 들었고 빠르기를 정했다(확인됨). 빈 환경에서 `tts_server.py` 설치 순서대로 깔아 본 적은 없다.
- **`kis_rsi.py setup`** — 입력을 받는 명령이라 끝까지 돌려 보지 못했다.

## 다음 단계

1. push (위 커밋들).
2. TODO.md 순서대로: 알림 채점(되감기) → 장 상태·알림 시간대 → 종목별 설정 → 여러 시간봉 RSI → 켤 때 등락률 채우기 → 자동 시작. 텔레그램은 나중.
3. 09-28 장중에 「아직 확인 못 한 것」을 본다.

알려진 작은 문제: 서버를 켜면 미국 종목 「등락」 칸이 첫 체결 전까지 비어 있다 (TODO 6).

## 파일

| 파일 | 하는 일 |
|---|---|
| `kis_rsi.py` | 키·토큰, 분봉(미국·국내), 실시간 웹소켓(`live`, `subscribe`), RSI·MACD, `Book`(봉 갱신), 지수·비트코인 조회, 터미널 명령 |
| `kis_web.py` | FastAPI 서버 `Hub`: 종목 목록, 실시간 루프, 알림·시그널 판정, 지수 띠, 기록, API(`/api/state`, `/api/chart/{symb}`, `/api/tickers`, `/api/sound`, `/api/signal-sound`, `/api/sound/test`, `/ws`) |
| `kis_alert.py` | 경고선·재무장·쿨다운(`Gate`), 읽을 문장(`say_breach`, `say_signal`, `phrases`), 목소리(`Voice`: 캐시, 로컬/Edge/SAPI, 줄 세워 재생) |
| `kis_signal.py` | 매수·매도 시그널 (`rsi_band`, `signals`) |
| `static/index.html` | 웹 화면 한 파일 (lightweight-charts 4.2.3, CDN) |
| `tts_server.py` | 로컬 Qwen3-TTS 서버 (선택) |
| `requirements.txt`, `README.md`, `TODO.md`, `NOTES.md`, `docs/screen_2026-09-25.png` | |

저장소에 안 올라가는 것(`.gitignore`): `kis_config.json`(키), `kis_token.json`, `kis_exchange.json`(거래소 캐시),
`kis_watchlist.json`(종목 목록), `kis_settings.json`(소리·인사), `kis_alerts.jsonl`(알림 기록), `tts_cache/`, `.venv*/`, `.claude/`.

## 시험할 때

- 키는 사용자 `~/.bashrc` 의 `KIS_APPKEY`/`KIS_APPSECRET`. 비대화형 셸은 `.bashrc` 를 안 읽으니 `source ~/.bashrc` 뒤에 돌린다.
- Git Bash 에서 한글 예외 문구를 찍을 때는 `PYTHONIOENCODING=utf-8`.
- 서버 띄우기: `source ~/.bashrc; python kis_web.py --no-browser` (백그라운드). 새 서버를 띄우기 전에 옛 것을 꼭 내린다(실시간 연결 하나).
- 알림 판정 시험은 `Hub` 를 만들어 가짜 `Book` 으로 `check_alerts`/`check_signals` 를 부른다. 이때 `kis_web.ALERT_LOG` 를 임시 파일로 바꿔 진짜 기록을 더럽히지 않는다.
- 소리는 AI 가 못 듣는다. 틀어 주고 길이·걸린 시간만 보고하고, 들렸는지는 사용자에게 묻는다.
