# Claude 가 이 저장소에서 일할 때

먼저 [NOTES.md](NOTES.md)(지금 상태·결정·시험하는 법)와 [TODO.md](TODO.md)를 읽는다. 사용자와의 말투도 NOTES.md 맨 위를 따른다.

## 한국투자증권 API 호출량 — 많이·자주 부를 일은 사용자에게 먼저 말한다

- **KIS Developers 이용약관**(2022-10-01 시행): 제9조 — 「회사의 서버에 일정수준 이상의 부하를 유발하는 등」이면 이용을 **중지**할 수 있고,
  고객은 10일 안에 소명해야 한다. 제10조 — 같은 까닭으로 소명이 안 되면 이용 승인을 **해지**할 수 있다.
  제12조 — 유량 제어 정책은 회사가 따로 정해 게시한다 (약관에 숫자는 없다).
- 흔히 알려진 한도(공식 공지는 못 찾음): 실전 REST 초당 20건, 모의 5건. 넘으면 `EGW00201`(초당 거래건수 초과). 접근토큰 발급은 1분에 1번.
- 우리 코드는 요청마다 0.1~0.12초 쉰다 (한 프로세스 초당 5건 남짓).
- **한꺼번에 많이, 또는 되풀이해 부를 일은 하기 전에 사용자에게 몇 번·얼마 동안인지 말하고 허락을 받는다.**
  예: 여러 종목 분봉을 거슬러 받기(`kis_report.py --fetch`, `kis_tf.py`·`kis_replay.py` 를 `--offline` 없이 전 종목), 쪽을 넘기며 받기,
  `kis_collect.json` 에 종목 더하기(처음 수집이 종목당 200번쯤), 서버에 새 주기 조회 넣기.
  현재가 하나·뉴스 한 쪽 같은 몇 번의 조회는 괜찮다. 이미 허락받은 예약 수집(30분마다 `--collect`)은 그대로 돈다.

## 그 밖의 규칙

- **실시간 연결은 앱키 하나에 하나** — 사용자가 띄운 서버가 쓰고 있으니 새 서버·`live`·`rsi`·`watch` 를 띄우지 않는다 (REST 조회는 된다).
  서버를 끄고 켤 일이 있으면 사용자에게 먼저 묻는다. 끄기 `python kis_web.py --stop`.
- **커밋은 기능 하나 끝날 때마다 묻지 않고 한다. push 는 사용자가 시킬 때만.**
- **저장소는 공개다.** 키·토큰·계좌, 사용자 개인 이야기는 올리지 않는다. 올리지 않을 노트는 `NOTES.local.md`(`.gitignore`). push 전에 `git grep` 으로 확인한다.
- 문서·주석·커밋 메시지는 쉬운 평서체. 마크다운의 범위 물결표는 `\~` 로 적는다 (깃허브가 취소선으로 긋는다).
- 규칙을 고치면 `python -m unittest discover tests`.
