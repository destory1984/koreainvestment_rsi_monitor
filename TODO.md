# 할 일

하나씩 만든다. 끝나면 줄을 긋지 말고 「한 것」으로 옮긴다.

## 할 것

### 4. 종목별 선
- 종목마다 선을 다르게 (변동이 큰 3배 ETF 는 25/75 같은 식). 10-02 채점을 보고 정한다.

### 9. 채점 결과 보고서 페이지
- 10-02 채점을 종목별·세션별로 한눈에 보는 웹 페이지.

### 나중에
- 주간거래 분봉 수집 시간 줄이기 (세션 끝난 뒤 한 번만 받아도 되는지 며칠 더 본다)
- 넥스트레이드 실시간 (국내 15:30 뒤)

## 한 것

- 분봉 캐시를 SQLite 로 — `replay_cache/bars.db`. 종목·기간이 늘어도 수집이 늘 빠르고, 수집과 되감기가 겹쳐도 안전하다.
- 텔레그램 알림 — 알림·시그널을 폰으로도. Webull 판 환경변수를 그대로 쓴다. 「⚙ 설정」에 스위치와 「보내 보기」.
- 규칙 시험 코드 — `python -m unittest discover tests`. 알림·시그널·소리 가리기·알림 뒤 결과·휴장일·국내 분봉·채점 29개.
- 휴장일 알기 — 장 상태가 공휴일에 「휴장 (휴일)」. 국내는 한국투자증권에 하루 한 번 묻고, 미국은 2027년까지 목록.
  「소리 나는 때」의 미국 세션 판단도 휴일을 안다.
- 알림 기록에 그 뒤 결과 — 줄마다 15·30·60분 뒤 가격이 알림 쪽으로 갔는지(+ 초록, − 빨강). 다시 켜도 남는다.
- 국내 RSI 를 켠 때와 상관없이 맞추기 — 국내 분봉을 KRX 정규장(09:00~15:30)만 쓴다. 넥스트레이드 애프터 봉이
  새로 켠 서버에만 있어 다음 날 RSI 가 달랐다. 되감기도 같이.
- 그 종목만 소리 끄기 — 줄 끝 종 단추. 끈 종목은 이름 앞에 🔇, 알림 기록에는 「🔇 종목 소리 끔」.
  설정 `mute`(종목 목록). 종목을 빼면 목록에서도 빠진다.
- 여러 시간봉 RSI — 「1·5·15·60」 칸. 주 분봉 말고 1·15·60분 Book 을 따로 두고 체결을 같이 넣는다.
  켤 때 뒤에서 받는다 (국내는 1분봉을 묶느라 종목당 10초 남짓).
- 켤 때 등락률 채우기 — 미국 종목은 켤 때 현재가를 한 번 묻는다 (주간거래 시간이면 BAQ 등).
  켤 때 이미 선 너머인 알림은 소리 없이 기록만 (「🔇 켤 때 이미 넘어 있음」). 다시 켤 때 알림이 몰리던 것.
- 자동 시작 — 로그인할 때 작업 스케줄러가 pythonw 로 창 없이 띄운다 (`--log`). 이미 떠 있으면 스스로 끝난다.
  키는 ~/.bashrc 에서도 읽는다. 주간거래 분봉 수집도 작업 스케줄러 (평일 09:00~18:30, 30분마다).
- 알림 시간대 — 세션마다(미국 주간·프리·정규·애프터, 국내) 소리 켜고 끄기, 조용한 시각. 가린 알림은 기록에 🔇.
- 장 상태 표시 — 지수 띠 맨 앞에 미국·한국 장 상태와 남은 시간 (화면에서 시각으로 가름, 공휴일은 모름).
- 알림 채점 (`kis_replay.py`) — 며칠 치 분봉을 받아 같은 규칙(Gate·signals)으로 되감고 15·30·60분 뒤를 센다.
  09-25 서버 알림 기록과 대조: 알림 15개 중 12개, 시그널 7개 중 6개가 같은 봉 (나머지도 대부분 2봉 안).
- 매수·매도 시그널 (`kis_signal.py`) — rsi_signal.py 규칙을 옮기고, 봉 안 RSI 최저·최고는 저가·고가로 계산.
  표의 「매수/매도」 칸, 차트 표시, 알림 기록, TTS. Webull 버전 시그널 15개 중 13개가 같은 봉에서 났다.
- Webull 화면 캡처 버전 내려놓기 — 같은 순간 견준 RSI 차이가 중앙값 0.18 이라 이쪽으로 옮겼다.
  판독용 qwen3.8:27b 가 쓰던 VRAM 약 17GB 가 빈다.
